import { Options } from '@vitejs/plugin-react';
import { Plugin } from 'vite';

declare type ReactPresetPlugin = {
    /** Disabled React devtools in production */
    removeDevtoolsInProd?: boolean;
    /** Inject `React` into every file to not declare `import React from 'react';` everywhere */
    injectReact?: boolean;
    /** Pass options as-it-is to @vitejs/plugin-react */
    reactPluginOptions?: Options;
};
declare function reactPlugin({ removeDevtoolsInProd, injectReact, reactPluginOptions: reactRefreshOptions, }?: ReactPresetPlugin): Plugin[];

export { reactPlugin as default };
